import { GlobalSearch } from "@/components/remaining-pages";
export const metadata = { title: "Search — Devflow AI", description: "Search your Devflow AI workspace." };
export default function Page() { return <GlobalSearch />; }
